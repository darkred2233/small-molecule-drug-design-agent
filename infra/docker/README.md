# Docker Assets

Target home for Dockerfiles and Compose fragments.

For now, the active Dockerfiles remain in the repository-level `docker/`
directory and the active Compose file remains `docker-compose.yml` at the
repository root.

