# MinIO Assets

Target home for bucket initialization, storage policies, and local object
storage setup notes.

Current runtime settings are read from `MEDAGENT_STORAGE_*` environment
variables in `.env`.

