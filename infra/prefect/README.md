# Prefect Assets

Target home for future workflow runner configuration.

The current MVP runs pipeline steps synchronously through FastAPI and
`PipelineOrchestrator`; no Prefect worker is required yet.

